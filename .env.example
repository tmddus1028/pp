LLM_PROVIDER=local
# Explicit claim-improvement button only: inherit/local/openai/azure.
# Keep LLM_PROVIDER=local and use IMPROVEMENT_PROVIDER=azure for on-demand AI review.
IMPROVEMENT_PROVIDER=inherit
# Set a Structured Outputs-capable model explicitly for your account.
OPENAI_API_KEY=
OPENAI_MODEL=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=
LLM_TIMEOUT_SECONDS=60
MAX_UPLOAD_MB=20
MAX_PDF_PAGES=150
MAX_DOCUMENT_CHARS=500000
OA_CHUNK_CHARS=14000
API_BASE_URL=http://127.0.0.1:8000
# Local OCR only. Blank command: PATH or standard Windows install locations.
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe
TESSERACT_CMD=
OCR_DPI=300
OCR_MIN_TEXT_CHARS=20
OCR_TIMEOUT_SECONDS=60
OCR_MAX_PIXELS=40000000
# auto: prefer PyMuPDF; PDFium only if PyMuPDF cannot load. pymupdf: strict mode.
OCR_RENDERER=auto
